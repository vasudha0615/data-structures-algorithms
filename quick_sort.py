def quick_sort(arr):
    partition(arr,0,len(arr))
    return arr

    partition(arr,start,end):
    for i in range(len(arr)):
    start = i
    pivot = arr[start]
    end = j
    while i < j and arr[pivot]<arr[start]:
    if arr[start]>arr[]
    
